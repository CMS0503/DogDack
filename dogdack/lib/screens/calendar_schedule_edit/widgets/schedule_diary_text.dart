import 'package:flutter/cupertino.dart';

class ScheduleDiaryText extends StatelessWidget {
  const ScheduleDiaryText({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('일기 쓰는 공간입니다'),
    );
  }
}
